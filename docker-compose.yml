services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: vestir
      POSTGRES_PASSWORD: vestir
      POSTGRES_DB: vestir
    ports:
      - '5434:5432'
    volumes:
      - vestir-pgdata:/var/lib/postgresql/data

volumes:
  vestir-pgdata:
