# Database (matches docker-compose.yml at the repo root)
DATABASE_URL="postgresql://vestir:vestir@localhost:5434/vestir?schema=public"

# Server
PORT=3000

# Local disk storage (Phase 1 dev stand-in for Cloudflare R2)
STORAGE_LOCAL_PATH=storage/garments
